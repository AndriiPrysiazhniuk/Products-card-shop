type PropsType = {
    title: string
    description: string
    unitPrice: number
    img: string
}
export const ProductCard = ({title, description, unitPrice, img}: PropsType) => {

    return (
        <div>

        </div>


    );
};

export default ProductCard;